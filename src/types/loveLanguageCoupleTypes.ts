// ========================================================
// TIPOS TYPESCRIPT — COMBINAÇÕES CONJUGAIS DE IDIOMAS DO AMOR
// ========================================================

export interface LoveLanguageCoupleContent {
  id?: string;
  numero_original?: number;
  idioma_pessoa: string;      // 'palavras_afirmacao' | 'atos_servico' | 'toque_fisico' | 'tempo_qualidade' | 'presentes'
  idioma_conjuge: string;     // 'palavras_afirmacao' | 'atos_servico' | 'toque_fisico' | 'tempo_qualidade' | 'presentes'
  nivel_compatibilidade: string;
  mapa_compatibilidade: string;
  ponto_forte: string;
  desafio_principal: string;
  reflexao_relacional: string;
  acoes_concretas: string;
  resumo: string;
}
